# Documentation Requirements

## Requirements

- Identify d-Matrix Corsair as the production AI inference accelerator platform/product built around d-Matrix's memory-centric compute architecture.
- Keep the accelerator/card identity distinct from complete rack systems, cloud services, and generic digital in-memory compute architecture concepts.
- Preserve form factor, memory/compute architecture, scale-out/interconnect, supported precision, and hardware specifications only where current first-party sources support the exact product scope.
- Treat production/shipping state, customer deployment, benchmark performance, software/runtime support, and system packaging as freshness-sensitive.
- Render the standard `entity-relations` block from validated current-entity relations.

## Validation

- Corsair remains a concrete inference hardware-product identity rather than a hosted service or generic architecture concept.
- Producer provenance resolves to d-Matrix.
