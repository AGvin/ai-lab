# Documentation Requirements

## Requirements

- Present `assistant-workspaces/` as the services catalog index for hosted conversational-assistant/workspace products.
- Render the standard child-navigation block from the validated direct-child projection so every materialized canonical assistant-workspace service appears exactly once.
- Keep underlying model identities, coding-agent products, installable software, and connected third-party applications with their respective canonical owners.

## Validation

- The child-navigation block matches the validated materialized direct-child projection and every destination resolves to a canonical hosted service profile.
- The category does not duplicate model or coding-agent identities.
