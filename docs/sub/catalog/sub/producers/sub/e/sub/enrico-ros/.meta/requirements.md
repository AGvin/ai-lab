# Documentation Requirements

## Requirements

- Identify Enrico Ros as the origin producer of Big-AGI.
- Preserve the current independent-project boundary without inventing a separate corporate producer from branding alone.
- Render the standard `entity-relations` block from the validated current-entity relation projection.
- Include current first-party profile and Big-AGI repository references.

## Validation

- Big-AGI provenance remains tied to verified project authorship rather than inferred company ownership.
- The `entity-relations` block matches the validated current-entity relation projection and every rendered destination resolves to a canonical node.
