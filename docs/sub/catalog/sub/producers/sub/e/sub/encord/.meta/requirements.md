# Documentation Requirements

## Requirements

- Identify Encord as the producer identity for the Encord managed data-development platform.
- Render the standard `entity-relations` block from validated current-entity relations.

## Validation

- `produces` is the inverse of Encord service `produced-by`.
