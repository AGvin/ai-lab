# Documentation Requirements

## Requirements

- Identify Snorkel AI as the canonical producer organization for Snorkel Flow.
- Keep concrete Snorkel Flow platform features, deployment topology, model/data workflows, and SDK behavior with the software entity.
- Render the standard `entity-relations` block from validated current-entity relations.

## Validation

- The `produces` relation is the inverse of Snorkel Flow `produced-by`.
