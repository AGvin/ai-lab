# Documentation Requirements

## Requirements

- Identify Scale AI as the producer identity for Scale Data Engine.
- Keep producer identity distinct from individual service modules and expert/data-production workflows.
- Render the standard `entity-relations` block from validated current-entity relations.

## Validation

- `produces` is the inverse of Scale Data Engine `produced-by`.
