# Documentation Requirements

## Requirements

- Identify SWE-agent as the project/team identity that publishes and maintains mini-SWE-agent and related SWE-agent research software.
- State the current upstream provenance conservatively: the project is built and maintained by researchers from Princeton University and Stanford University.
- Do not invent a separate legal-company form for the SWE-agent project/team.
- Link the canonical mini-SWE-agent Local Agent profile from the physically materialized `produces` relation when the reciprocal mini-SWE-agent `produced-by` relation resolves successfully.
- Include current official mini-SWE-agent documentation and SWE-agent GitHub organization references.

## Validation

- The page distinguishes the SWE-agent producer/team identity from the mini-SWE-agent software entity.
- Princeton/Stanford affiliation is described as upstream team provenance rather than legal ownership.
- The SWE-agent/mini-SWE-agent `produces` / `produced-by` relation pair is physically present at both endpoints and semantically consistent.
- The mini-SWE-agent link resolves to the canonical Local Agent profile.
