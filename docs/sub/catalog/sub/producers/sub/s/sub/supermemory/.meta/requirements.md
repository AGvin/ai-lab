# Documentation Requirements

## Requirements

- Identify Supermemory as the producer identity for the Supermemory managed memory/context service.
- Render the standard `entity-relations` block from validated current-entity relations.

## Validation

- `produces` is the inverse of Supermemory service `produced-by`.
