# Documentation Requirements

## Requirements

- Identify Zep as the producer identity for the Zep managed memory/context service.
- Render the standard `entity-relations` block from validated current-entity relations.

## Validation

- `produces` is the inverse of Zep service `produced-by`.
