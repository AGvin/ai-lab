# Documentation Requirements

## Requirements

- Identify Guru Technologies, Inc. as the organization producing Guru.
- Keep the organization identity distinct from the Guru hosted knowledge-workspace service.
- Render the standard `entity-relations` block from the validated current-entity relation projection.
- Include current first-party Guru company/product and legal references.

## Validation

- The organization and service identities are not collapsed.
- The `entity-relations` block matches the validated current-entity relation projection and every rendered destination resolves to a canonical node.
