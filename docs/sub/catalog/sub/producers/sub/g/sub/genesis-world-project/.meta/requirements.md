# Documentation Requirements

## Requirements

- Use a project-level Genesis World producer identity for the software without inferring corporate ownership from current support language.
- Record Genesis AI support only when relevant and source-backed.
- Render the standard `entity-relations` block from validated current-entity relations.

## Validation

- `produces` is the inverse of Genesis World `produced-by`.
