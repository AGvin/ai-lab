# Documentation Requirements

## Requirements

- Identify Vibrant Labs as the organization/team behind the represented Ragas evaluation framework and its current upstream organization.
- Keep the producer organization distinct from Ragas software and from Vibrant Labs' broader research/benchmark work.
- Render the standard `entity-relations` block from the validated current-entity relation projection.
- Include current first-party Vibrant Labs and Ragas repository references.

## Validation

- The page does not collapse Vibrant Labs and Ragas into one entity.
- The `entity-relations` block matches the validated current-entity relation projection and every rendered destination resolves to a canonical node.
- Producer claims remain bounded to current first-party evidence.
