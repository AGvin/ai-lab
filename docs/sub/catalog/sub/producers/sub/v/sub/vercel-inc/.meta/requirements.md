# Documentation Requirements

## Requirements

- Identify Vercel Inc. as the organization behind the represented Vercel AI SDK.
- Keep the producer organization distinct from the AI SDK software identity and from Vercel AI Gateway or other hosted Vercel services.
- Render the standard `entity-relations` block from the validated current-entity relation projection.
- Include current official Vercel legal, product, and AI SDK references supporting provenance.

## Validation

- The page does not collapse Vercel Inc., Vercel AI SDK, and hosted Vercel AI services into one entity.
- The `entity-relations` block matches the validated current-entity relation projection and every rendered destination resolves to a canonical node.
- Official resource links match canonical entity metadata.
