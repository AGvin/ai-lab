# Documentation Requirements

## Requirements

- Identify the verl Project/community as the current direct producer identity for verl.
- Do not infer ByteDance as the current direct producer solely from project-origin history.
- Render the standard `entity-relations` block from validated current-entity relations.

## Validation

- `produces` is the inverse of verl `produced-by`.
