# Documentation Requirements

## Requirements

- Identify Portkey, Inc. as the organization behind the represented Portkey AI Gateway.
- Keep the producer identity distinct from the open-source gateway runtime, hosted gateway operation, and the broader Portkey commercial AI-control platform.
- Render the standard `entity-relations` block from the validated current-entity relation projection.
- Include current first-party legal, product, and repository references supporting provenance.

## Validation

- Portkey, Inc., the open-source gateway, and separately operated Portkey services are not collapsed into one entity.
- The producer relation does not imply that the software itself owns downstream model-provider APIs.
- The `entity-relations` block matches the validated current-entity relation projection and every rendered destination resolves to a canonical node.
