# Documentation Requirements

## Requirements

- Identify rUv (`ruvnet`) as the individual publishing the represented RuVector project through the current upstream GitHub repository.
- Keep the person identity distinct from RuVector software and from optional hosted/shared-memory services referenced by the project.
- Render the standard `entity-relations` block from the validated current-entity relation projection.
- Include the official GitHub profile and RuVector repository as identity/provenance references.

## Validation

- The page does not collapse rUv and RuVector into one entity.
- The `entity-relations` block matches the validated current-entity relation projection and every rendered destination resolves to a canonical node.
- The displayed producer name remains tied to the current upstream profile identity rather than being inferred from the repository handle alone.
