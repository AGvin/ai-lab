# Documentation Requirements

## Requirements

- Identify Okta as the producer identity for Okta for AI Agents.
- Keep the agent-specific product distinct from Okta's broader workforce/customer identity portfolio.
- Render the standard `entity-relations` block from validated current-entity relations.

## Validation

- `produces` is the inverse of Okta for AI Agents `produced-by`.
