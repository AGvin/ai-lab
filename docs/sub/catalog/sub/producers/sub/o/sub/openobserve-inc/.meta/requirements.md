# Documentation Requirements

## Requirements

- Identify OpenObserve Inc. as the canonical producer organization for OpenObserve.
- Use current first-party legal material for company identity and keep product-specific facts with the OpenObserve software owner.
- Render the standard `entity-relations` block from validated relations.

## Validation

- The producer profile does not duplicate OpenObserve product documentation.
- The `produces` / `produced-by` relation pair remains symmetric.
