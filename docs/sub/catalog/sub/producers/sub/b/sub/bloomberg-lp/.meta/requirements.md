# Documentation Requirements

## Requirements

- Identify Bloomberg L.P. as one of the two organizations that initiated the open collaboration that became Envoy AI Gateway.
- Preserve joint project origin with Tetrate without implying that Bloomberg alone owns or currently maintains the multi-organization project.
- Render the standard `entity-relations` block from the validated current-entity relation projection.
- Include current Bloomberg identity evidence and the official Envoy AI Gateway origin reference.

## Validation

- Bloomberg's origin role is not generalized into sole ownership or sole maintenance of Envoy AI Gateway.
- Tetrate's co-origin role remains distinct and compatible with current multi-organization maintenance.
- The `entity-relations` block matches the validated current-entity relation projection and every rendered destination resolves to a canonical node.
