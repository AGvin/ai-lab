# Documentation Requirements

## Requirements

- Identify Berrie AI Incorporated as the organization behind LiteLLM, preserving `BerriAI` as the upstream repository/organization spelling where useful.
- Keep the producer identity distinct from the LiteLLM software product and from separately operated hosted or enterprise service surfaces.
- Render the standard `entity-relations` block from the validated current-entity relation projection.
- Include current first-party site and repository references supporting identity and provenance.

## Validation

- The organization and LiteLLM software are not collapsed into one entity.
- Hosted/enterprise operation is not implied by the producer relation alone.
- The `entity-relations` block matches the validated current-entity relation projection and every rendered destination resolves to a canonical node.
