# Documentation Requirements

## Requirements

- Identify Boomi as the producer identity for Boomi Agent Control Plane.
- Keep the organization profile distinct from Agent Control Plane, Agentstudio, Boomi Connect, AI Gateway, Orchestrate, Meta Hub, Runtime, and other Boomi platform products.
- Render the standard `entity-relations` block from validated current-entity relations.

## Validation

- `produces` is the inverse of Boomi Agent Control Plane `produced-by`.
