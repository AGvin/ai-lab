# Documentation Requirements

## Requirements

- Identify Builder Methods as the organization publishing the represented Agent OS project.
- Keep the producer organization distinct from the Agent OS software entity.
- Render the standard `entity-relations` block from the validated current-entity relation projection.
- Include the current official Builder Methods organization and Agent OS repository references.

## Validation

- The page does not collapse Builder Methods and Agent OS into one entity.
- The `entity-relations` block matches the validated current-entity relation projection and every rendered destination resolves to a canonical node.
- Producer claims remain bounded to current upstream organization/repository evidence.
