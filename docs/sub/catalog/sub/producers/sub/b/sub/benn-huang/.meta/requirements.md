# Documentation Requirements

## Requirements

- Identify Benn Huang (`bennhuang`, `Bin-Huang`) as the original producer/creator of Chatbox.
- Keep original authorship distinct from the current Mediocre, LLC stewardship layer and community contributions.
- Render the standard `entity-relations` block from the validated current-entity relation projection.
- Include current first-party author/profile and Chatbox repository references.

## Validation

- Benn Huang remains the origin producer without implying sole current maintenance or corporate ownership.
- The `entity-relations` block matches the validated current-entity relation projection and every rendered destination resolves to a canonical node.
