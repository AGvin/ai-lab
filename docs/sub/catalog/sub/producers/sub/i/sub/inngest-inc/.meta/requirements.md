# Documentation Requirements

## Requirements

- Identify Inngest Inc as the company behind the Inngest event-driven durable-execution platform and open-source software surface.
- Keep the company identity distinct from the self-hosted software and the managed Inngest service boundary.
- Render the standard `entity-relations` block from the validated current-entity relation projection.
- Include current first-party Inngest legal, open-source, and product references.

## Validation

- Inngest Inc is not conflated with either the canonical software entity or the managed service surface.
- The `entity-relations` block matches the validated current-entity relation projection and every rendered destination resolves to a canonical node.
