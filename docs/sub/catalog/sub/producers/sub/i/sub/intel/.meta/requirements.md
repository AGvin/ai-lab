# Documentation Requirements

## Requirements

- Identify Intel as the producer identity for Intel Gaudi 3 hardware represented in the catalog.
- Keep product-specific specifications/benchmarks with the hardware profile rather than duplicating them at producer scope.
- Render the standard `entity-relations` block from validated current-entity relations.

## Validation

- `produces` is the inverse of Intel Gaudi 3 `produced-by`.
