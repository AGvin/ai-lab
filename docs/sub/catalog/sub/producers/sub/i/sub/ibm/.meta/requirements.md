# Documentation Requirements

## Requirements

- Identify IBM as the producer identity for IBM commercial AI products represented in the catalog.
- Keep IBM distinct from IBM Research when a research-group identity independently produces research artifacts.
- Render the standard `entity-relations` block from validated current-entity relations.

## Validation

- `produces` is the inverse of IBM watsonx.governance `produced-by`.
- IBM and IBM Research remain separate producer identities unless a specific relation is source-backed and required.
