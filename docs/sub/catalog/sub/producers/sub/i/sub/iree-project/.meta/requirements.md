# Documentation Requirements

## Requirements

- Identify the IREE Project as the project-level producer identity for IREE.
- Do not infer corporate ownership from contributor or hosting relationships without separate authoritative evidence.
- Render the standard `entity-relations` block from validated current-entity relations.

## Validation

- `produces` is the inverse of IREE `produced-by`.
