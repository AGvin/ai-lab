# Documentation Requirements

## Requirements

- Identify n8n GmbH as the organization providing and developing the represented n8n workflow-automation software.
- Keep the producer organization distinct from the n8n software identity and from n8n Cloud or other separately operated service surfaces.
- Render the standard `entity-relations` block from the validated current-entity relation projection.
- Include current official legal, site, and repository references supporting provenance.

## Validation

- The page does not collapse n8n GmbH, self-hosted n8n software, and n8n Cloud into one entity.
- The `entity-relations` block matches the validated current-entity relation projection and every rendered destination resolves to a canonical node.
- Legal identity remains source-backed by current first-party terms.
