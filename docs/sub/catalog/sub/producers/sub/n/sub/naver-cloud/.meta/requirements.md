# Documentation Requirements

## Requirements

- Identify NAVER Cloud as the producer identity for selected HyperCLOVA X model releases.
- Keep NAVER Corp., hosted CLOVA services, cloud infrastructure, and exact model checkpoints separate from this producer profile.
- Render the standard `entity-relations` block from validated current-entity relations.

## Validation

- `produces` is the inverse of HyperCLOVA X SEED `produced-by`.
