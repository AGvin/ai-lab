# Documentation Requirements

## Requirements

- Identify H3 Labs Inc. as the company behind Maxim AI and the represented Bifrost AI gateway.
- Preserve `Maxim AI` as the product/company brand without replacing the source-backed H3 Labs Inc. organization identity.
- Keep the producer organization distinct from Bifrost software and separately operated Maxim platform/service surfaces.
- Render the standard `entity-relations` block from the validated current-entity relation projection.
- Include current first-party legal/privacy, Bifrost, and repository references supporting provenance.

## Validation

- H3 Labs Inc., Maxim AI branding, and Bifrost software are not incorrectly materialized as unrelated producers.
- The producer relation does not imply that every Maxim AI service belongs to the Bifrost software identity.
- The `entity-relations` block matches the validated current-entity relation projection and every rendered destination resolves to a canonical node.
