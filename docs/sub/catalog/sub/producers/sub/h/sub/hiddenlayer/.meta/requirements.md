# Documentation Requirements

## Requirements

- Identify HiddenLayer as the producer identity for HiddenLayer AI Security Platform.
- Render the standard `entity-relations` block from validated current-entity relations.

## Validation

- `produces` is the inverse of HiddenLayer AI Security Platform `produced-by`.
