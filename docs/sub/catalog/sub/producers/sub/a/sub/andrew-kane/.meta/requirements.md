# Documentation Requirements

## Requirements

- Identify Andrew Kane (`ankane`) as the individual producer/maintainer associated with pgvector in the official upstream repository metadata.
- Keep the person identity distinct from the pgvector software entity and from hosted PostgreSQL providers that distribute the extension.
- Render the standard `entity-relations` block from the validated current-entity relation projection.
- Include Andrew Kane's official GitHub profile and the official pgvector repository as provenance references.

## Validation

- The page does not collapse Andrew Kane, pgvector, and hosted PostgreSQL providers into one entity.
- The `entity-relations` block matches the validated current-entity relation projection and every rendered destination resolves to a canonical node.
- Producer claims remain bounded to current upstream evidence.
