# Documentation Requirements

## Requirements

- Identify Activepieces Inc. as the organization behind the represented Activepieces automation platform.
- Keep the producer organization distinct from the Activepieces software entity and from separately operated hosted/enterprise service surfaces.
- Render the standard `entity-relations` block from the validated current-entity relation projection.
- Include current official legal, site, and repository references supporting organization identity and product provenance.

## Validation

- The page does not collapse Activepieces Inc. and Activepieces software into one entity.
- Hosted/enterprise service boundaries remain distinct from the canonical software profile.
- The `entity-relations` block matches the validated current-entity relation projection and every rendered destination resolves to a canonical node.
