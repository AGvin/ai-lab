# Documentation Requirements

## Requirements

- Identify API Hero Ltd as the legal company trading as Trigger.dev and behind the Trigger.dev open-source workflow platform.
- Keep the company identity distinct from the self-hosted Trigger.dev software and from Trigger.dev Cloud/managed execution services.
- Render the standard `entity-relations` block from the validated current-entity relation projection.
- Include current first-party Trigger.dev legal and product references.

## Validation

- API Hero Ltd is not conflated with either the canonical Trigger.dev software entity or its cloud service surface.
- The `entity-relations` block matches the validated current-entity relation projection and every rendered destination resolves to a canonical node.
