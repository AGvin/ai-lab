# Documentation Requirements

## Requirements

- Identify Axolotl AI Cloud as the upstream organization producing Axolotl for catalog provenance.
- Keep any hosted Axolotl cloud/service offering distinct unless independently materialized later.
- Render the standard `entity-relations` block from validated current-entity relations.

## Validation

- `produces` is the inverse of Axolotl `produced-by`.
