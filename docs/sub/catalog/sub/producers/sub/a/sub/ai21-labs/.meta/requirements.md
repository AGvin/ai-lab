# Documentation Requirements

## Requirements

- Identify AI21 Labs as the producer identity for Jamba2.
- Keep company identity distinct from AI21 Studio, API plans, applications, and exact model checkpoints.
- Render the standard `entity-relations` block from validated current-entity relations.

## Validation

- `produces` is the inverse of Jamba2 `produced-by`.
