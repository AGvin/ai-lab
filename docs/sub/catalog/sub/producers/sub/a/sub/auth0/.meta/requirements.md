# Documentation Requirements

## Requirements

- Identify Auth0 as the product/producer identity for Auth0 for AI Agents.
- Keep parent-company ownership and broader Auth0 platform facts separate unless needed for current provenance.
- Render the standard `entity-relations` block from validated current-entity relations.

## Validation

- `produces` is the inverse of Auth0 for AI Agents `produced-by`.
