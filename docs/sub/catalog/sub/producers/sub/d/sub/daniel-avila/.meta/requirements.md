# Documentation Requirements

## Requirements

- Identify Daniel Avila (`davila7`) as the maintainer and producer associated with Claude Code Templates, using the project's first-party repository and maintainer identity as the evidence boundary.
- Keep Daniel Avila distinct from the existing Danny Avila producer entity associated with LibreChat; the similar names do not represent one canonical person.
- Render the standard `entity-relations` block from the validated current-entity relation projection.
- Include the current official GitHub profile and Claude Code Templates repository as first-party identification/provenance resources.

## Validation

- The page resolves to `catalog/producers/d/daniel-avila`, not `catalog/producers/d/danny-avila`.
- Claude Code Templates provenance is represented without attributing LibreChat or Danny Avila's work to Daniel Avila.
- The `entity-relations` block matches the validated current-entity relation projection and every rendered destination resolves to a canonical node.
