# Documentation Requirements

## Requirements

- Identify Descope as the producer identity for Agentic Identity Hub.
- Render the standard `entity-relations` block from validated current-entity relations.

## Validation

- `produces` is the inverse of Descope Agentic Identity Hub `produced-by`.
