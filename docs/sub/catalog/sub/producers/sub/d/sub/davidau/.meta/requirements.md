# Documentation Requirements

## Requirements

- Identify David Belton as the individual publishing models under the `DavidAU` Hugging Face handle.
- Link the DavidAU model-domain view for navigation and link the represented derivative model through the `produces` relation.
- Preserve model-specific merge, fine-tune, license, context, and artifact facts on exact model/artifact pages rather than the producer profile.
- Include the current Hugging Face profile and linked GitHub account as authoritative identity references.

## Validation

- `DavidAU` is represented as a publishing alias, not a separate organization.
- Producer and model links resolve to canonical catalog nodes.
- Provider/model claims are not generalized into producer-level technical facts.
