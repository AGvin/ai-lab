# Documentation Requirements

## Requirements

- Identify Danny Avila as the founder and original producer of LibreChat.
- Keep LibreChat's origin distinct from its current ClickHouse ownership layer and from the broader LibreChat team/community.
- Render the standard `entity-relations` block from the validated current-entity relation projection.
- Include current first-party profile and acquisition/provenance references.

## Validation

- Danny Avila is represented as LibreChat's founder/origin producer without implying sole current ownership or maintenance.
- ClickHouse's later acquisition does not replace LibreChat's original producer provenance.
- The `entity-relations` block matches the validated current-entity relation projection and every rendered destination resolves to a canonical node.
