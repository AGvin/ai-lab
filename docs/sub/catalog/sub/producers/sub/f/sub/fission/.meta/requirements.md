# Documentation Requirements

## Requirements

- Identify Fission as the organization publishing the represented OpenSpec project through the current `Fission-AI` upstream.
- Keep the producer organization distinct from the OpenSpec software entity.
- Render the standard `entity-relations` block from the validated current-entity relation projection.
- Include the current official Fission organization and OpenSpec repository references.

## Validation

- The page does not collapse Fission and OpenSpec into one entity.
- The `entity-relations` block matches the validated current-entity relation projection and every rendered destination resolves to a canonical node.
- The producer name remains tied to the current upstream organization identity.
