# Documentation Requirements

## Requirements

- Identify LG AI Research as the producer identity for K-EXAONE.
- Keep the organization profile distinct from LG group companies, applications, hosted services, and exact checkpoints.
- Render the standard `entity-relations` block from validated current-entity relations.

## Validation

- `produces` is the inverse of K-EXAONE `produced-by`.
