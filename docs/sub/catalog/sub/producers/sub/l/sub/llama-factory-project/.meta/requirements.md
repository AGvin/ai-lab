# Documentation Requirements

## Requirements

- Use a project-level producer identity for LLaMA Factory rather than inferring a company or using personal repository ownership as the catalog's organization identity.
- Render the standard `entity-relations` block from validated current-entity relations.

## Validation

- `produces` is the inverse of LLaMA Factory `produced-by`.
