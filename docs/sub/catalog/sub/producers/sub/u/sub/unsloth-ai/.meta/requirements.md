# Documentation Requirements

## Requirements

- Identify Unsloth AI as the upstream organization/product producer for Unsloth.
- Keep future independently material hosted products separate from the current installable software identity when deployment warrants it.
- Render the standard `entity-relations` block from validated current-entity relations.

## Validation

- `produces` is the inverse of Unsloth `produced-by`.
