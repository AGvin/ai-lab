# Documentation Requirements

## Requirements

- Identify UC Berkeley Sky Computing Lab as the research lab where vLLM was originally developed.
- Keep vLLM origin distinct from current community-driven development and current PyTorch Foundation / Linux Foundation hosting.
- Render the standard `entity-relations` block from the validated current-entity relation projection.
- Include current Sky Computing Lab and vLLM project references.

## Validation

- The lab is represented as vLLM's producer/origin without implying sole current maintenance or governance.
- Current foundation hosting is not converted into a retroactive origin claim.
- The `entity-relations` block matches the validated current-entity relation projection and every rendered destination resolves to a canonical node.
