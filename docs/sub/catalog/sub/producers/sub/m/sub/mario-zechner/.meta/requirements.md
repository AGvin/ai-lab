# Documentation Requirements

## Requirements

- Identify Mario Zechner (`badlogic`) as the original/current author and producer of the Pi agent-harness/coding-agent project.
- Keep creator provenance distinct from the May 2026 Earendil Works organizational/package stewardship transition.
- Render the standard `entity-relations` block from the validated current-entity relation projection.
- Include current upstream Pi package-author metadata and first-party identity references.

## Validation

- Mario Zechner is not presented as a substitute for the current Earendil organizational stewardship layer.
- Creator provenance and current organization/package scope remain distinct.
- The `entity-relations` block matches the validated current-entity relation projection and every rendered destination resolves to a canonical node.
