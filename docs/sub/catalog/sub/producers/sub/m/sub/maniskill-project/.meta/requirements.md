# Documentation Requirements

## Requirements

- Use a project-level producer identity for ManiSkill unless a separate current corporate producer is authoritatively established.
- Render the standard `entity-relations` block from validated current-entity relations.

## Validation

- `produces` is the inverse of ManiSkill `produced-by`.
