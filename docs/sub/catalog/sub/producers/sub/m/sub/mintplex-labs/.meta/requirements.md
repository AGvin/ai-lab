# Documentation Requirements

## Requirements

- Identify Mintplex Labs as the organization producing AnythingLLM.
- Keep the organization distinct from the AnythingLLM software identity and its hosted/self-hosted operating surfaces.
- Render the standard `entity-relations` block from the validated current-entity relation projection.
- Include current first-party Mintplex Labs and AnythingLLM references.

## Validation

- Do not add an unsupported legal suffix to the organization name.
- The `entity-relations` block matches the validated current-entity relation projection and every rendered destination resolves to a canonical node.
