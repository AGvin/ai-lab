# Documentation Requirements

## Requirements

- Identify ModelOp as the producer identity for ModelOp Enterprise AI Command Center.
- Keep the product's current deployment contract with its software profile rather than duplicating it as a service identity.
- Render the standard `entity-relations` block from validated current-entity relations.

## Validation

- `produces` is the inverse of ModelOp Enterprise AI Command Center `produced-by`.
