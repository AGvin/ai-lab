# Documentation Requirements

## Requirements

- Identify Mediocre, LLC as the organization operating and producing the current Chatbox AI product surface.
- Keep the legal organization distinct from the Chatbox client software and hosted Chatbox AI model service.
- Render the standard `entity-relations` block from the validated current-entity relation projection.
- Include current first-party Chatbox product/support references.

## Validation

- The page does not infer a different company identity from older generic Chatbox terms wording.
- The `entity-relations` block matches the validated current-entity relation projection and every rendered destination resolves to a canonical node.
