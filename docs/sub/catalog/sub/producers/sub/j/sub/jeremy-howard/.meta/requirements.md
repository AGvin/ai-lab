# Documentation Requirements

## Requirements

- Identify Jeremy Howard as the author of the `llms.txt` proposal using the current upstream proposal metadata.
- Keep his person identity distinct from Answer.AI, the `AnswerDotAI/llms-txt` repository organization, documentation platforms that implement the convention, and the formal proposal artifact itself.
- Do not infer ownership, maintenance, organizational hierarchy, or other relations from repository hosting alone.
- Render the standard `entity-relations` block from the validated current-entity relation projection.
- Include Jeremy Howard's official site and the current upstream proposal repository as provenance references.

## Validation

- The `produces` relation resolves to `catalog/specifications/formats/llms-txt` and is matched by the proposal artifact's inverse `produced-by` relation.
- Author attribution remains source-backed and does not imply unsupported organizational or ownership relations.
- The `entity-relations` block matches the validated current-entity relation projection.
