# Documentation Requirements

## Requirements

- Identify Cisco as the producer identity for Cisco AI Defense.
- Keep the AI Defense service distinct from the broader Cisco cybersecurity portfolio.
- Render the standard `entity-relations` block from validated current-entity relations.

## Validation

- `produces` is the inverse of Cisco AI Defense `produced-by`.
