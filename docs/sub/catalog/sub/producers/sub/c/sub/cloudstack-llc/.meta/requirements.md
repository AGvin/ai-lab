# Documentation Requirements

## Requirements

- Identify CloudStack, LLC as the organization behind Msty Studio.
- Keep the organization distinct from the Msty Studio product and related Msty services/products.
- Render the standard `entity-relations` block from the validated current-entity relation projection.
- Include the current first-party Msty legal reference.

## Validation

- The page does not treat the Msty brand as a separate legal organization without evidence.
- The `entity-relations` block matches the validated current-entity relation projection and every rendered destination resolves to a canonical node.
