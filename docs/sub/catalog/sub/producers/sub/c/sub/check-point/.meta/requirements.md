# Documentation Requirements

## Requirements

- Identify Check Point as the producer identity for Check Point AI Agent Security.
- Preserve acquired/product-brand lineage only when current first-party evidence makes it relevant; do not duplicate current security products by legacy branding.
- Render the standard `entity-relations` block from validated current-entity relations.

## Validation

- `produces` is the inverse of Check Point AI Agent Security `produced-by`.
