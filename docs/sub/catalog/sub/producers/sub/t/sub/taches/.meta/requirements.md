# Documentation Requirements

## Requirements

- Identify TÂCHES as the original named author/producer of Get Shit Done (GSD) from the first-party archived project metadata.
- Keep original production distinct from the later Open GSD maintenance/stewardship role.
- Render the standard `entity-relations` block from the validated current-entity relation projection.
- Include the archived upstream repository and its package metadata as provenance evidence.

## Validation

- The page does not claim TÂCHES is the current maintainer solely from original authorship.
- The `entity-relations` block matches the validated current-entity relation projection and every rendered destination resolves to a canonical node.
- The person classification remains bounded to the first-party named-author evidence rather than asserting an unverified legal identity.
