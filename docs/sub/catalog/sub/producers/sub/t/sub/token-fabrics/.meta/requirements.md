# Documentation Requirements

## Requirements

- Identify Token Fabrics as the current organization operating the hosted Big-AGI product surface.
- Keep current operation distinct from Enrico Ros's origin/producer role and from the open-source Big-AGI software identity.
- Do not add an unsupported `LLC` suffix or infer ownership solely from current branding/copyright presentation.
- Render the standard `entity-relations` block from the validated current-entity relation projection.
- Include current first-party Big-AGI site and repository references.

## Validation

- Token Fabrics is represented as operator rather than as an unverified original producer or legal owner.
- The `entity-relations` block matches the validated current-entity relation projection and every rendered destination resolves to a canonical node.
