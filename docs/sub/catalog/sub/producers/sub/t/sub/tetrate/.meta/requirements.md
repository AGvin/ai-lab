# Documentation Requirements

## Requirements

- Identify Tetrate as one of the two organizations that initiated the open collaboration that became Envoy AI Gateway.
- Preserve joint project origin with Bloomberg without implying that Tetrate alone owns or currently maintains the multi-organization project.
- Render the standard `entity-relations` block from the validated current-entity relation projection.
- Include current Tetrate identity material and the official Envoy AI Gateway origin reference.

## Validation

- Tetrate's origin role is not generalized into sole ownership or sole maintenance of Envoy AI Gateway.
- Bloomberg's co-origin role remains distinct and compatible with current multi-organization maintenance.
- The `entity-relations` block matches the validated current-entity relation projection and every rendered destination resolves to a canonical node.
