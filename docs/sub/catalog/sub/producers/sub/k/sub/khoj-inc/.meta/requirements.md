# Documentation Requirements

## Requirements

- Identify Khoj Inc. as the organization producing and providing Khoj.
- Keep the organization distinct from Khoj software and Khoj Cloud as operating/product surfaces.
- Render the standard `entity-relations` block from the validated current-entity relation projection.
- Include current first-party Khoj legal and product references.

## Validation

- The organization and software identities are not collapsed.
- The `entity-relations` block matches the validated current-entity relation projection and every rendered destination resolves to a canonical node.
