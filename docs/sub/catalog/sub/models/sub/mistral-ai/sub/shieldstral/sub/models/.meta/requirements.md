# Documentation Requirements

## Requirements

- List only materialized concrete trained models in the Shieldstral family.
- Keep hosted aliases, quantizations, deployment artifacts, and moderation-service routes outside the concrete-model collection.

## Validation

- Every child is an independently identifiable trained model release rather than a serving alias or artifact.
