# Documentation Requirements

## Requirements

- Identify `pplx-embed-v1-0.6b` as the current 0.6B-class standard PPLX Embed V1 model for independent texts, queries, and sentences.
- Preserve the current 1024-dimensional, 32K-context identity and supported reduced-output encodings only while first-party Perplexity documentation supports those values.
- Keep hosted pricing, quotas, batching, API response encoding, and similarity/search workflow recommendations freshness-sensitive.

## Validation

- The model remains a standard embedding member, not the contextualized 0.6B sibling.
- Hosted API behavior is not treated as permanent intrinsic architecture.
