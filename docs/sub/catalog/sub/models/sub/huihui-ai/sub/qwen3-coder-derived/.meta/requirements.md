# Documentation Requirements

## Requirements

- Treat this node as navigation for reviewed huihui.ai models with documented Qwen3-Coder-derived lineage.
- Do not type the grouping as a model family or series unless an authoritative source establishes such an identity.
- Link only materialized concrete models.
- Keep exact base lineage and modification method on concrete model pages.

## Validation

- Shared upstream ancestry is not represented as shared model identity.
- Every listed child is a concrete trained/modified model rather than an artifact.
