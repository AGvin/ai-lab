# Documentation Requirements

## Requirements

- Treat this node as navigation for reviewed DavidAU models with documented Qwen3-derived lineage.
- Do not type the grouping as a model family or series unless an authoritative source establishes such an identity.
- Link only materialized concrete models.
- Keep exact merge/fine-tune lineage on concrete model pages.

## Validation

- Shared upstream ancestry is not represented as shared model identity.
- Every listed child is a concrete trained/merged model rather than an artifact.
