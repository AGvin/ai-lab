# Documentation Requirements

## Requirements

- List only reviewed concrete trained model identities in the Z-Image family.
- Keep variants unmaterialized until exact model content is required.
- Keep quantizations, conversions, hosted endpoints, runtime presets, and selection routes outside the concrete-model collection.

## Validation

- Every child is a distinct trained model identity rather than an artifact or service alias.
