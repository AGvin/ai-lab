# Documentation Requirements

## Requirements

- List only reviewed concrete trained model identities in the Jev line.
- Keep API aliases, hosted endpoints, SDK names, service plans, and mutable deployment revisions outside the concrete-model collection unless they independently satisfy the model/version identity contract.

## Validation

- Every child represents a trained model identity rather than a provider alias, hosted service, or API route.
