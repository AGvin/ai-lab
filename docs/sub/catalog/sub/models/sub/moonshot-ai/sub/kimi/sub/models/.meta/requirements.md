# Documentation Requirements

## Requirements

- List only materialized concrete trained models in the Kimi family.
- Keep hosted aliases, access products, quantized/distribution artifacts, and deployment variants outside the concrete-model collection.

## Validation

- Every child is a distinct model identity rather than an artifact, service alias, or product surface.
