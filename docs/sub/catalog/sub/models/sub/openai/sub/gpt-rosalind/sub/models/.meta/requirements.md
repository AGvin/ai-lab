# Documentation Requirements

## Requirements

- List only concrete GPT-Rosalind models explicitly identified by current first-party OpenAI material.
- Keep product/workspace releases, trusted-access cohorts, and benchmark configurations outside the concrete-model collection.

## Validation

- Every child is an explicitly named model identity rather than an inferred release snapshot.
