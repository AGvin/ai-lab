# Documentation Requirements

## Requirements

- List only released concrete Muse model identities backed by current first-party Meta evidence.
- Keep preview-only models and product/API routes outside the released concrete-model collection.

## Validation

- Every child passes the stable/released lifecycle gate independently.
