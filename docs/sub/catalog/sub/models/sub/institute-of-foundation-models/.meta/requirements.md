# Documentation Requirements

## Requirements

- Identify this node as the Institute of Foundation Models model-domain publishing/steward namespace.
- Keep producer organization facts with `catalog/producers/i/institute-of-foundation-models` and model-family/model facts with their canonical children.
- Keep hosted inference/provider routes and artifact packaging outside canonical trained-model identity.

## Validation

- The page remains model-domain navigation/context and does not duplicate the producer organization profile.
