# Documentation Requirements

## Requirements

- Identify Grok as xAI's model family and keep it distinct from the Grok hosted assistant workspace.
- Link xAI through the canonical `produced-by` relation.
- Link Grok 4.6 as the currently materialized concrete Grok model without inventing an intermediate series level that current evidence does not require.
- Keep mutable API pricing, access tiers, routing aliases, and product behavior outside stable family identity.

## Validation

- The Grok assistant service is not treated as a model alias.
- Concrete Grok specifications are not generalized across the whole family.
- Every producer and model membership relation resolves to a canonical node.
