# Documentation Requirements

## Requirements

- Identify `command-r7b-12-2024` as the compact live Command R-series model.
- Keep context, RAG/tool-use positioning, availability, and deployment claims freshness-sensitive.

## Validation

- The model remains a concrete member of Command R.
