# Documentation Requirements

## Requirements

- Identify `embed-v4.0` as the current Embed 4 multimodal embedding model.
- Keep supported modalities/languages, context, embedding dimensions, provider availability, and API behavior freshness-sensitive.

## Validation

- The model remains a concrete Embed member and not a retrieval service.
