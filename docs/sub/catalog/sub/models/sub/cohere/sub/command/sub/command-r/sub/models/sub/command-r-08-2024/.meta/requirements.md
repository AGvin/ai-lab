# Documentation Requirements

## Requirements

- Identify `command-r-08-2024` as the current dated Command R model rather than the deprecated undated alias.
- Keep provider deployment identifiers and availability freshness-sensitive.

## Validation

- The model remains a concrete member of Command R.
