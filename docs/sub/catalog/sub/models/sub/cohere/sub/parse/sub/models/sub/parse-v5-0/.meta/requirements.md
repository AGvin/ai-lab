# Documentation Requirements

## Requirements

- Identify `parse-v5.0` as the live concrete Parse 5 vision-language model.
- Keep extraction formats, supported file types, parameter/model-size facts, endpoints, and cloud availability freshness-sensitive.

## Validation

- The model remains distinct from the Parse API and Compass product.
