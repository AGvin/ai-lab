# Documentation Requirements

## Requirements

- Identify Command R as the series grouping Cohere's live R, R+, and R7B concrete models.
- Preserve dated model IDs and avoid materializing deprecated aliases as peers.
- Keep deployment/provider IDs and pricing outside stable series identity.

## Validation

- Current concrete models link back to Command R and the series links to Command.
