# Documentation Requirements

## Requirements

- Identify Command A as Cohere's documented model series and link Command A+ as the currently materialized final model in that series.
- Keep concrete parameter counts, context, modalities, language support, and release facts with the concrete model owner.
- Keep hosted deployment, pricing, rate limits, and benchmark conclusions outside stable series identity.

## Validation

- Command A+ remains a concrete model within Command A rather than an alias for the series.
