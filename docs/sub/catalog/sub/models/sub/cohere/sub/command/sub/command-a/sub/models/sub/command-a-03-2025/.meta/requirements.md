# Documentation Requirements

## Requirements

- Identify `command-a-03-2025` as the live concrete Command A model.
- Keep context, tool-use, agent, RAG, deployment, and availability claims source-attributed and freshness-sensitive.

## Validation

- The model remains a member of the Command A series rather than a separate family.
