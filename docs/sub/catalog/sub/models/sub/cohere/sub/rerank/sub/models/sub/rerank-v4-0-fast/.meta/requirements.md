# Documentation Requirements

## Requirements

- Identify `rerank-v4.0-fast` as the latency/throughput-oriented concrete Rerank 4 model.
- Keep context, language coverage, structured-data support, endpoint limits, and performance claims freshness-sensitive.

## Validation

- The model remains distinct from the Pro variant while sharing the Rerank family.
