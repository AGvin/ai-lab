# Documentation Requirements

## Requirements

- List only materialized concrete trained models in the Mistral Small family.
- Keep artifacts, hosted aliases, and deployment variants outside the concrete-model collection.

## Validation

- Every child is a distinct model identity rather than an artifact or service alias.
