# Documentation Requirements

## Requirements

- Use `GSD Core` as the current page title and identify it as the active continuation of Get Shit Done (GSD).
- Describe GSD Core as a meta-prompting, context-engineering, and spec-driven development system that coordinates supported AI coding agents.
- Preserve its primary placement under `development-tools/agentic-development-systems`; it supplies planning/execution/verification workflows and persistent project state above host agents rather than replacing them as the coding-agent runtime.
- Preserve TÂCHES as the original producer through `produced-by` and Open GSD as the current maintainer through `maintained-by`; do not collapse origin and current stewardship.
- Preserve the file-based project-state and fresh-context design boundary at a stable conceptual level; keep exact commands, supported agents, configuration keys, and workflow internals source-backed when expanded.
- Include the current Open GSD / GSD Core repository and the archived original repository as continuity/provenance evidence.

## Validation

- The page distinguishes GSD Core from a standalone coding agent, general agent framework, and generic workflow engine.
- The page does not present the archived `gsd-build/get-shit-done` repository as the current development home.
- The TÂCHES and Open GSD links resolve to their canonical producer profiles with distinct producer/maintainer roles.
- The page does not imply that GSD Core itself provides the underlying model or coding-agent runtime.
- Official resource links match canonical entity metadata.
- The page contains no temporary-placeholder wording.
