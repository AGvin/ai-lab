# Cursor Agent Skills

This profile documents Cursor-specific Agent Skills support as mutable product facts while keeping Cursor canonically a code editor/development tool.

## Scope and boundaries

- The profile covers current project and user skill locations or scopes, discovery, invocation or automatic selection behavior, installation or creation flow, and relevant permissions or Agent execution boundaries when source-backed.
- Cursor Agent Skills remains distinct from rules, commands, subagents, hooks, plugins, MCP, and VS Code customization behavior.
- The profile keeps exact paths, supported metadata, commands, precedence, client/version availability, and interaction with Cursor Agent current-source-backed because they may change.

## Official resources

- <https://cursor.com/docs/context/skills>
