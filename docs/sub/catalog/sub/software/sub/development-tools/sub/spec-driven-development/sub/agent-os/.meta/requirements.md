# Documentation Requirements

## Requirements

- Identify current Agent OS as a spec-driven development support system centered on establishing codebase standards, injecting relevant standards into AI-tool context, and shaping stronger specifications.
- Preserve its placement under `development-tools/spec-driven-development`; current Agent OS works alongside AI coding tools rather than acting as their replacement runtime.
- Preserve Builder Methods as the canonical producer through the `produced-by` relation.
- Do not describe retired pre-v3 implementation-orchestration or bundled-subagent behavior as a current defining capability.
- Keep exact commands, profile/configuration mechanics, supported tools, and version-specific behavior source-backed when expanded.
- Include the current official Agent OS repository reference.

## Validation

- The page reflects the current standards-injection and spec-shaping focus rather than obsolete orchestration behavior.
- The page distinguishes Agent OS from a standalone coding agent and from a generic agent framework.
- The producer link resolves to the canonical Builder Methods profile.
- Official resource links match canonical entity metadata.
- The page contains no temporary-placeholder wording.
