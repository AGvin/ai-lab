# Documentation Requirements

## Requirements

- Group substantial Cursor-specific integration and extension surfaces without changing Cursor's canonical identity as a code editor/development tool.
- Keep concrete installation, discovery, invocation, permissions, configuration, and removal behavior with the corresponding integration child.
- Distinguish Cursor-owned integration behavior from VS Code compatibility and from generic integration concepts.
- Materialize integration children only when they have substantive Cursor-specific content.
