# Documentation Requirements

## Requirements

- Identify Trigger.dev as an open-source framework/platform for reliable background jobs, scheduled tasks, long-running workflows, and AI-oriented asynchronous application work.
- Preserve its primary placement under `workflow-engines/application-orchestration`; tasks run application code with durable/retry/observability infrastructure rather than defining a data-pipeline-specific DAG system.
- Preserve API Hero Ltd as the legal company trading as Trigger.dev and behind the open-source platform while keeping company, self-hosted software, and managed service identities distinct.
- Distinguish Trigger.dev Cloud from self-hosted Trigger.dev software and keep deployment/version architecture source-backed.
- Render the standard `entity-relations` block from the validated current-entity relation projection.
- Keep runtime support, machine/execution features, wait/checkpoint behavior, hosting architecture, and other mutable details source-backed when expanded.
- Include current official Trigger.dev documentation, repository, and legal references.

## Validation

- The page presents Trigger.dev as application/background-job orchestration rather than a generic AI-agent framework.
- API Hero Ltd is not conflated with the self-hosted software or managed service surface.
- Cloud and self-hosted operating surfaces are not conflated.
- The `entity-relations` block matches the validated current-entity relation projection and every rendered destination resolves to a canonical node.
- Official resource links match canonical entity metadata.
- The page contains no temporary-placeholder wording.
