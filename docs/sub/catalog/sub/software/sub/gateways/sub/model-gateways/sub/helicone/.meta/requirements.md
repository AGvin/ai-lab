# Documentation Requirements

## Requirements

- Identify Helicone as an open-source AI/model gateway platform whose request path unifies model-provider access, routing/fallback, and observability.
- Cover its gateway, routing, fallback, caching/rate-limit, prompt, analytics/observability, evaluation-feedback, and self-hosting surfaces only from current sources.
- Preserve the current lifecycle accurately: Helicone was acquired by Mintlify on 2026-03-03; its services remain live in maintenance mode with security, model, bug, and performance updates, and current signup availability must remain source-backed.
- Do not treat maintenance mode as abandonment, but do not present Helicone as an actively expanding peer without current evidence.
- Keep supported providers, model counts, pricing, signup state, and hosted availability freshness-sensitive.

## Validation

- The canonical placement reflects the gateway's position in the request path while still documenting the substantial observability identity.
- Lifecycle language matches the current first-party acquisition statement.
