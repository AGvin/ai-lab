# Documentation Requirements

## Requirements

- Identify Higress as an open-source, cloud-native API gateway based on Istio and Envoy with first-class AI-gateway capabilities.
- Preserve its primary placement under `gateways/api-management-gateways`; model/LLM/MCP routing is an important current capability within a broader API-gateway product.
- Render the standard `entity-relations` block from the validated current-entity relation projection.
- Distinguish the open-source Higress software from Alibaba Cloud's managed/commercial gateway products built from or around Higress.
- Keep model/provider counts, plugins, deployment modes, commercial guarantees, and other mutable claims source-backed when expanded.
- Include current official Higress documentation, repository, and first-party Alibaba provenance reference.

## Validation

- The page preserves Higress's broader API-gateway identity while acknowledging its AI-native functionality.
- Open-source software and Alibaba-operated commercial services are not conflated.
- Official resource links match canonical entity metadata.
- The page contains no temporary-placeholder wording.
- The `entity-relations` block matches the validated current-entity relation projection and every rendered destination resolves to a canonical node.
