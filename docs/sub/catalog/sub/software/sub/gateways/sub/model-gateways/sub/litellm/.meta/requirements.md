# Documentation Requirements

## Requirements

- Identify LiteLLM as an open-source AI gateway and Python SDK that provides a unified interface to many model providers, commonly through OpenAI-compatible formats.
- Preserve its primary placement under `gateways/model-gateways`; the centralized proxy/gateway role is the canonical software-navigation owner while the Python SDK remains part of the same product identity.
- Distinguish self-hosted LiteLLM gateway software from hosted proxy and enterprise service surfaces.
- Keep provider counts, endpoint coverage, performance figures, enterprise features, and other mutable implementation details source-backed when expanded.
- Include current official LiteLLM documentation and repository references.

## Validation

- The page captures both gateway/proxy and SDK surfaces without duplicating the product.
- Self-hosted and hosted operating boundaries are not conflated.
- Official resource links match canonical entity metadata.
- The page contains no temporary-placeholder wording.
