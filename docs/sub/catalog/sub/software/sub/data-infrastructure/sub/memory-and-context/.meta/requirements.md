# Documentation Requirements

## Requirements

- Present Memory and Context as the canonical data-infrastructure group for self-managed systems whose primary product role is persistent or reusable memory/context for AI applications and agents.
- List every materialized direct child exactly once.
- Keep underlying vector, graph, relational, embedding, or storage mechanisms as technical properties rather than alternate canonical placements when memory/context is the primary identity.
- Distinguish reusable memory infrastructure from full agent frameworks and from user-facing knowledge workspaces.

## Validation

- Navigation includes RuVector exactly once.
- The page remains category-level and does not duplicate the concrete product profile.
- The page contains no temporary-placeholder wording.
