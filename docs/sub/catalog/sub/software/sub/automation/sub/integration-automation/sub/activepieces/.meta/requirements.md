# Documentation Requirements

## Requirements

- Identify Activepieces as an open-source, extensible automation platform for building integration flows from triggers/actions and reusable pieces, with support for AI-enabled automation.
- Preserve its primary placement under `automation/integration-automation`; individual pieces are integration building blocks rather than separate canonical software products in this catalog.
- Preserve self-hosting as an available software boundary while keeping hosted/enterprise service and edition details distinct and source-backed.
- Render the standard `entity-relations` block from the validated current-entity relation projection.
- Keep piece counts, supported integrations, edition features, and other mutable platform facts source-backed when expanded.
- Include current official Activepieces site, documentation, and repository references.

## Validation

- The page presents Activepieces as integration-automation software rather than a hosted-only service or generic AI-agent framework.
- Self-hosted and hosted operating boundaries are not conflated.
- Official resource links match canonical entity metadata.
- The page contains no temporary-placeholder wording.
- The `entity-relations` block matches the validated current-entity relation projection and every rendered destination resolves to a canonical node.
