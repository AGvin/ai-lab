# Documentation Requirements

## Requirements

- Identify DVC (Data Version Control) as an open-source, Git-oriented command-line tool for versioning data/model artifacts, defining reproducible data/ML pipelines, managing experiments, and synchronizing large artifacts with remote storage.
- Preserve its primary placement under `model-and-data-platforms/versioning-and-lineage`; DVC extends Git workflows with data/model provenance and pipeline state rather than replacing Git itself or acting as a general experiment-tracking server.
- Preserve Dmitry Petrov as DVC's original creator/producer and preserve the documented November 2025 transition from Iterative.ai to lakeFS/Treeverse as current lifecycle context.
- Preserve local cache plus configurable remote-storage behavior as a stable boundary while keeping supported backends, experiment features, and other mutable details source-backed when expanded.
- Distinguish current DVC CLI/software from lakeFS and from separately operated historical Iterative commercial surfaces.
- Render the standard `entity-relations` block from the validated current-entity relation projection.
- Include current official DVC documentation, current Treeverse-hosted repository, creator provenance, and transition references.

## Validation

- The page preserves DVC's Git-oriented data/model versioning and reproducibility role.
- Dmitry Petrov's creator provenance remains distinct from later corporate stewardship.
- DVC remains a separate open-source project/brand and is not conflated with lakeFS.
- The `entity-relations` block matches the validated current-entity relation projection and every rendered destination resolves to a canonical node.
- Official resource links match canonical entity metadata.
- The page contains no temporary-placeholder wording.
