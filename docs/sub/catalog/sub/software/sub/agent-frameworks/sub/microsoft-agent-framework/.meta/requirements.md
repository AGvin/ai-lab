# Documentation Requirements

## Requirements

- Identify Microsoft Agent Framework as the canonical software profile for Microsoft's open-source agent framework represented at this path.
- Link Microsoft through the canonical `produced-by` relation.
- Cover agents and workflows as distinct framework surfaces; keep provider support, language support, hosting, integrations, and migration paths source-backed when expanded.
- Include official Microsoft Learn documentation and repository references.
- Preserve current migration relationships from AutoGen and Semantic Kernel only as source-backed migration guidance, not as identity equivalence.

## Validation

- Official resource links match canonical entity metadata.
- The page does not conflate Microsoft Agent Framework with Semantic Kernel or AutoGen.
- The page contains no temporary-placeholder wording.
