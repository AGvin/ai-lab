# Documentation Requirements

## Requirements

- Identify Ollama as the canonical software profile for the local/self-managed model runtime represented at this path.
- Include the official Ollama website as an identity and research reference.
- Keep installation, runtime configuration, supported features, platform behavior, and mutable compatibility facts source-backed when expanded.
- Keep concrete model identities and model-selection guidance with their respective canonical owners.

## Validation

- The official resource link matches canonical entity metadata.
- The page does not duplicate concrete model profiles or recommendation conclusions.
- Effective metadata does not depend on the legacy `meta.yml` file.
