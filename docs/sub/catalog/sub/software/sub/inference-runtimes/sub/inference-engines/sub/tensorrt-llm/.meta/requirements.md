# Documentation Requirements

## Requirements

- Identify TensorRT-LLM as NVIDIA's open-source library/runtime for accelerating and optimizing LLM inference on NVIDIA GPUs.
- Preserve its primary placement under `inference-runtimes/inference-engines`; integrations with NVIDIA Dynamo, Triton, and broader NVIDIA serving stacks do not make those systems the same product.
- Render the standard `entity-relations` block from the validated current-entity relation projection.
- Keep supported models, GPU generations, quantization/parallelism features, performance claims, and release-specific API details source-backed when expanded.
- Include current official TensorRT-LLM documentation and repository references.

## Validation

- The page distinguishes TensorRT-LLM from NVIDIA Dynamo and Triton Inference Server.
- Hardware or performance support is not generalized beyond current source evidence.
- Official resource links match canonical entity metadata.
- The page contains no temporary-placeholder wording.
- The `entity-relations` block matches the validated current-entity relation projection and every rendered destination resolves to a canonical node.
