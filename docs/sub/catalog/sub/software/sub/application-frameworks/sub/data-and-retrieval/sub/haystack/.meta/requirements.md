# Documentation Requirements

## Requirements

- Identify Haystack as an open-source AI orchestration framework for building production-oriented agents, RAG applications, multimodal search, and other LLM applications from reusable components and pipelines.
- Preserve its primary placement under `application-frameworks/data-and-retrieval`; retrieval/search remains a major domain while current Haystack also owns agent and orchestration abstractions.
- Render the standard `entity-relations` block from the validated current-entity relation projection.
- Preserve the distinction between open-source Haystack and deepset-operated commercial/enterprise platform surfaces.
- Keep component inventories, integrations, supported providers/document stores, enterprise features, and release-specific behavior source-backed when expanded.
- Include current official Haystack documentation and repository references.

## Validation

- The page reflects current agent/orchestration scope rather than reducing Haystack to legacy search or RAG-only positioning.
- Open-source framework and commercial platform boundaries are not conflated.
- Official resource links match canonical entity metadata.
- The page contains no temporary-placeholder wording.
- The `entity-relations` block matches the validated current-entity relation projection and every rendered destination resolves to a canonical node.
