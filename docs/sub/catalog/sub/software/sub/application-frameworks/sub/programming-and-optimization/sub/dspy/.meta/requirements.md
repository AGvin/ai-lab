# Documentation Requirements

## Requirements

- Identify DSPy as an open-source Python framework for programming language-model systems with structured signatures/modules and optimizing those programs against explicit metrics.
- Preserve its primary placement under `application-frameworks/programming-and-optimization`; DSPy's defining boundary is programmatic composition plus optimizer-driven improvement rather than manual prompt-template engineering.
- Render the standard `entity-relations` block from the validated current-entity relation projection.
- Preserve the distinction between DSPy program modules and DSPy optimizers that tune instructions, demonstrations, and in some workflows model weights.
- Keep optimizer names, model/provider adapters, version requirements, benchmark claims, and other mutable implementation details source-backed when expanded.
- Include current official DSPy documentation and repository references.

## Validation

- The page reflects the current “program, don’t prompt” abstraction without presenting it as literal absence of prompts inside execution.
- Optimizer behavior is not overstated beyond current source support.
- Official resource links match canonical entity metadata.
- The page contains no temporary-placeholder wording.
- The `entity-relations` block matches the validated current-entity relation projection and every rendered destination resolves to a canonical node.
