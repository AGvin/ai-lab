# Documentation Requirements

## Requirements

- Identify Ragas as an open-source evaluation toolkit for building systematic evaluation loops around LLM applications.
- Preserve its primary placement under `evaluation-and-observability/evaluation-frameworks`; RAG evaluation remains an important use case but is not the only current application boundary.
- Preserve current upstream repository identity under `vibrantlabsai/ragas`.
- Render the standard `entity-relations` block from the validated current-entity relation projection.
- Keep metric inventories, test-generation capabilities, integrations, telemetry behavior, and other mutable details source-backed when expanded.
- Include current official Ragas documentation and repository references.

## Validation

- The page does not reduce current Ragas to RAG-only evaluation.
- Current upstream repository ownership is used.
- Official resource links match canonical entity metadata.
- The page contains no temporary-placeholder wording.
- The `entity-relations` block matches the validated current-entity relation projection and every rendered destination resolves to a canonical node.
