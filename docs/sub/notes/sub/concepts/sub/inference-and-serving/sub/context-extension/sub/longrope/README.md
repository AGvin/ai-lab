# LongRoPE

LongRoPE is a context-window extension method for RoPE-based language models that uses non-uniform positional interpolation and searched rescaling factors to support substantially longer sequences.
