# Position Interpolation

Position Interpolation is a context-window extension method that rescales position indices so longer sequences fit within a model's original positional range, allowing RoPE-based language models to adapt to longer contexts.
