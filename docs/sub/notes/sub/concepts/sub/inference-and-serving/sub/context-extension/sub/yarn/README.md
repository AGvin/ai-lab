# YaRN

YaRN (Yet another RoPE extensioN method) is a context-window extension method for transformer language models that use Rotary Position Embeddings (RoPE). It adapts RoPE-based positional handling so models can operate on sequence lengths beyond their original context window.
