<!--
l10n:
  locale: uk_UA
  source_locale: default
  source_path: ../../README.md
  source_hash: gitblob:ef3f57d8967cd991ca2ea562231c344021766b29
  mode: translated
-->

# mini-SWE-agent

Мінімальний агент програмної інженерії для локальних або ізольованих coding-задач, керованих через shell.

## Переклади

- [English](../../)
- Українська

## Метадані

```text
Тип ресурсу: агент програмної інженерії
Основний сценарій використання: розв’язувати задачі програмної інженерії шляхом редагування репозиторіїв, виконання shell-команд і тестів та створення patches у локальних або ізольованих середовищах
Модель доступу: інструмент командного рядка з відкритим кодом, локальним використанням і обліковими даними зовнішнього постачальника моделей
Ліцензія: MIT
Модель вихідного коду: відкритий код
Операційні вимоги: пакет mini-SWE-agent, локальне або ізольоване середовище виконання, цільовий репозиторій чи робочий простір задачі, shell-доступ, облікові дані вибраного постачальника моделей і необов’язкові benchmark harnesses
Режими інтеграції: CLI, локальні репозиторії, shell-команди, subprocess execution, локальні середовища, Docker, Podman, Singularity, Apptainer, bubblewrap, contree, LiteLLM, OpenRouter, Portkey, робочі процеси оцінювання на кшталт SWE-bench
Джерело: https://github.com/SWE-agent/mini-swe-agent
Примітки щодо ризиків: автономний coding-agent, керований через shell; перед використанням із приватними репозиторіями чи чутливими даними перевірте доступ на запис, виконання команд і тестів, межі sandbox, створені patches, облікові дані постачальника, розкриття секретів і припущення benchmark-versus-production.
```

## Огляд

mini-SWE-agent — мінімальний агент програмної інженерії для coding-задач, які можна виконувати через shell у локальному або ізольованому середовищі.

Він не обмежений GitHub issues, але його основна область вужча, ніж у універсального асистента чи IDE coding-agent. Він зосереджений на задачах програмної інженерії: перегляді репозиторію, редагуванні файлів, виконанні команд або тестів, діагностиці збоїв і створенні patch.

Проєкт навмисно зберігає scaffold агента малим. Типова архітектура покладається на bash замість великого набору спеціалізованих інструментів, використовує незалежні subprocess-виклики для дій і зберігає лінійну історію взаємодії, яку легше переглядати, діагностувати або адаптувати до benchmark- і дослідницьких процесів.

## Відповідність AI Lab

mini-SWE-agent належить до `agents/`, оскільки його основна задокументована роль — агентоподібна система для автономної або напівавтономної роботи з програмної інженерії.

Це не фреймворк оркестрації агентів. Використовуйте `agent-orchestration/` для фреймворків, runtimes або control planes, що координують кілька агентів чи агентних процесів.

Використовуйте mini-SWE-agent як орієнтир для:

- мінімальної архітектури агента програмної інженерії;
- редагування репозиторіїв і виконання тестів через shell;
- локальних або ізольованих процесів coding-agent;
- benchmark- і дослідницьких процесів на кшталт SWE-bench;
- порівняння з ширшими coding agents, як-от Aider, Cline, Goose, Claude Code і Codex CLI;
- оцінювання потрібного обсягу agent scaffold навколо спроможної моделі.

## Примітки щодо оцінювання

Перед впровадженням оцініть:

- чи відповідає задача програмній інженерії через shell, а не pair programming у стилі IDE;
- стратегію локального середовища, Docker, Podman, bubblewrap або іншого sandbox;
- межі запису до репозиторію та review створених patches;
- ризики виконання shell-команд і тестів;
- підтримку постачальника моделей і оброблення облікових даних;
- розкриття вихідного коду, журналів, помилок і секретів;
- придатність до production-процесів порівняно з benchmark- або дослідницьким використанням.

## Джерела

- GitHub mini-SWE-agent: https://github.com/SWE-agent/mini-swe-agent
- Документація mini-SWE-agent: https://mini-swe-agent.com/latest/
