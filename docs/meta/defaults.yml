node:
  schema: root

  requirements:
    path: requirements/README.md

  template:
    layout:
      id: default

  children:
    schema: default

  localization:
    schema: localized
    default_locale: en_US
    locales:
      - uk_UA
